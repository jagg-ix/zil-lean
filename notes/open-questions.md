# Open Questions

1. Path query syntax: recursive-only vs dedicated path operators.
2. Quantifiers in constraints: keep minimal or add `FORALL` syntax in core.
3. Conflict between ordered rule clauses and purely declarative semantics.
4. Default consistency profile selection for deployments.

